version: '3.8'

services:
  web:
    build: .
    container_name: wedd_app_web
    ports:
      - "8010:8000"
    command: sh -c "python manage.py collectstatic --noinput && python manage.py runserver 0.0.0.0:8000"
    restart: unless-stopped
